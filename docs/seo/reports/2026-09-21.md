# Weekly SEO review, 2026-09-21

## What moved

Nothing to compare this week. No new snapshot was produced, so there are no fresh numbers
against last week (still the 2026-09-16 baseline: 60 ranked keywords, 70 referring domains,
103 clicks / 33,464 impressions over 28 days, position 22.3).

Why: all six environment variables the snapshot script needs are missing again
(DATAFORSEO_LOGIN, DATAFORSEO_PASSWORD, PAGESPEED_API_KEY, GBP_CLIENT_ID, GBP_CLIENT_SECRET,
GOOGLE_SEO_REFRESH_TOKEN), so DataForSEO and Search Console/GA4 could not run. This session's
own network policy also would not let it reach lawnandlandmarketing.com or api.dataforseo.com
at all (blocked with a 403 at the egress proxy), so the site crawl, which normally needs no
credentials, could not run either. PageSpeed Insights failed too, not from a missing key but
because the shared free quota was already exhausted (429). Every data source was blocked or
empty this week; past weeks could at least crawl the site and run PageSpeed.

## What I fixed

With the live site unreachable, I checked the repo's own files and generators instead:

- Added the missing contextual CTA link line to 5 posts: green-industry-marketing-agency,
  nate-precision-landscape-management-scalable-growth, the-5-hour-weekly-marketing-blueprint...,
  why-500-leads-in-10-days..., and your-2025-digital-marketing-plan.
- Trimmed 4 meta descriptions over 158 characters (green-industry-marketing-agency,
  marketing-tool-that-outperforms, seo-for-lawn-care-companies, what-a-lawn-care-marketing-
  agency-does), updating the meta tag, Open Graph, Twitter card, and matching JSON-LD in each.
- Confirmed all 48 posts have a seoTitle under 60 characters, the sitemap already lists every
  post in _blog.json with nothing missing either direction, and every FAQ block parses cleanly.
- Ran the full pipeline (gen_blog.py, gen_blog_schema.py, build.py, build.py --check,
  check_links.py, check_structured_data.py). Everything passes: 125 files with no broken asset
  references, 203 valid JSON-LD blocks, 114 pages in sync with the shared header/footer.
- Checked for open seo/ pull requests: none exist, so nothing to merge.

## What needs Matt

1. The environment variables from last week are still not set on the cloud session (same ask
   as 2026-09-16 and 2026-09-20).
2. New this week: the session's network access is blocking the production domain and
   DataForSEO outright, separate from the missing keys. Another session hit the same wall
   yesterday and logged it in STRATEGY.md with a recommendation to move both weekly routines
   to GitHub Actions in this repo, the same pattern Competitor Radar already uses. I agree with
   that recommendation: setting the env vars alone will not fix the network block.
3. Two older posts (best-green-industry-marketing-agencies, land-clearing-marketing) have no
   in-article CTA block at all, only the site-wide band at the bottom of every page. Adding one
   means writing new copy, not a mechanical fix, so I left them for a content pass.
