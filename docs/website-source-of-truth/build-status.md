# Build Status

Last updated: 2026-06-13.

## Overall read
Two things are materially developed now: the **homepage** and the **full set of 8
marketing-services detail pages** (built on a locked, reusable template). The rest of the
non-home pages are still intentional shells awaiting design. Route hygiene is clean and the
build tooling (universal header/footer + service-page generator) is in place.

## What is DONE
- **Homepage** — strongest single page; hero, proof, program framing, differentiation, FAQs.
- **All 8 `/marketing-services/*` detail pages** — built on the locked template
  (`docs/website-source-of-truth/service-page-template.md`). Per page: hero, conviction
  stat (real + sourced), showcase + labeled image placeholder, "What's included" (purple
  featured card + 7 Lucide-icon cards), second showcase, FAQ (placeholder answers), and the
  Twilight-spotlight CTA. Service JSON-LD on every page; all internal links resolve.
- **Marketing-services HUB** (`/marketing-services/`) — the SEO silo page: hero + "one
  machine" framing + 4 featured services (image+text sections) + a wrap-up grid linking the
  other 4 + Twilight CTA. Links all 8 detail pages; `ItemList` schema. Hand-built (not
  generated by `gen_service.py`).
- **Universal header + footer** — single source via `build.py` (`_header.html`/`_footer.html`).
- **Twilight spotlight + dot-grid CTA** — brand pattern, on all service CTAs.
- **Nav submenu hover** — fixed with a transparent hover-bridge (no more vanishing menus).
- **Body text legibility** — global `--muted` lightened for readability.

## What is NOT done
- These remain shells (header + hero + blank body + CTA/footer): `/about/`, `/contact/`,
  `/industries/*`, `/resources/*`.
- `/programs/` + `/programs/*` — older lorem layout prototype; NOT yet on the new template.
- The 8 service pages need three **owner inputs** to be truly finished (below).

## Owner inputs needed to finalize the 8 service pages
1. **FAQ answers** — 5 per page, currently `[NEEDS YOUR INPUT]` placeholders. Once real,
   add `FAQPage` JSON-LD to each page.
2. **Conviction stats** — verify the 7 industry stats (each has a `statSource` in
   `_content.json`).
3. **Images** — replace the labeled placeholders (each says exactly what to drop in).

## Highest-priority work next
1. Land the owner inputs above → mark the 8 service pages "finished".
2. Build the **Program pages** on the same template approach.
3. Design + build the remaining non-home pages (About, Contact, services hub, Industries,
   Resources).
4. Eventually: launch cutover to lawnandlandmarketing.com (not yet).

## Success criteria for a "finished" page
Live ≠ finished. A finished page has: a clear strategic purpose, a strong H1/promise,
enough body to persuade and rank, proof/trust where appropriate, clear CTA paths, correct
canonical internal links — and **no invented facts** (placeholders for anything unverified).
